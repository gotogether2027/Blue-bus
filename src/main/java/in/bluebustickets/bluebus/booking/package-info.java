/**
 * Inventory sale occupancy and booking module boundary.
 * Physical trip seat inventory is snapshotted in scheduling. Future occupancy is
 * {@code TripSeatAllocation} over half-open stop ranges; holds and bookings are not implemented yet.
 */
package in.bluebustickets.bluebus.booking;
