package in.bluebustickets.bluebus.scheduling.application;

/**
 * Customer-journey availability for one physical seat on a requested OD segment.
 * Derived — never stored on inventory.
 */
public enum JourneySeatAvailability {
    AVAILABLE,
    UNAVAILABLE
}
