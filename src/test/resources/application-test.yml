spring:
  application:
    name: blue-bus-backend-test
  main:
    banner-mode: "off"

blue-bus:
  security:
    jwt:
      issuer: test-issuer
      secret: test-only-placeholder-not-a-secret
  seat-holds:
    expiry:
      enabled: false
      reaper-interval-ms: 60000
      batch-size: 50
